# 2026-03-27 Session Memory

## 重大決策

### 資料庫遷移：InfluxDB → TimescaleDB [P0]
- Phase A~D 完成，TimescaleDB 為唯一資料源
- Schema：backtest_runs, equity_curve, trade_blotter, strategy_signals, strategy_performance, ohlcv
- backtest_runs.mode 欄位區分 backtest / sim / live
- DSN 從環境變數讀取（`TIMESCALE_DSN`）
- InfluxDB fallback 全移除，所有路徑單一直連

### Grafana 三板架構 [P0]
- Backtest：歷史回測，run_id 動態選擇（SQL query backtest_runs）
- Monitor：scheduler 每小時訊號追蹤（mode=sim）
- Live：實盤 placeholder，待接下單 API
- 命名統一：Backtest / Monitor / Live，無 tags
- 全部使用 TimescaleDB SQL datasource

### No-fallback 規範 [P0]
- 加入 AGENTS.md：所有資料源直連，連線失敗直接報錯
- 不做自動 fallback，保持程式碼單一路徑

### Market Config 兩層架構 [P1]
- `config/markets.yaml`：市場層（crypto/tw_futures/us_equity）+ 標的層
- 市場層：時區、交易時段、計價貨幣
- 標的層：tick_size, tick_value, commission_rate, min_commission, slippage_ticks, exchange, data_source
- 回測引擎從 InstrumentConfig 取成本，不再 hardcode

### Streamlit 定位 [P1]
- 改為策略研究工具（vectorbt 參數掃描）
- 不做即時監控（已搬到 Grafana）
- 不引入 MLRun，直接用 TimescaleDB 存參數掃描結果

## 技術狀態

- TimescaleDB: localhost:5432 (container: quant_timescaledb)
- InfluxDB: 保留 container 但不再寫入，可後續移除
- Grafana: http://35.194.150.232:3000（admin/admin）
- Streamlit: http://35.194.150.232:8502（systemd service）
- Scheduler: scripts/monitor/scheduler.py（每小時整點，nohup 跑中）

## 待處理

- InfluxDB container 可以退役（scheduler/backtest 已不寫入）
- Streamlit 改版為策略研究工具（下一里程碑）
- vectorbt 參數掃描介面
- Public Dashboard 設定（第三方觀測用）
