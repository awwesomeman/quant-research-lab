# 2026-03-26 Daily Log

## 重大發現：OpenClaw subagent 設定一直沒用到
- openclaw.json 裡已設好 backend/frontend subagent，但我一直用 Claude CLI 跑
- 正確做法：`sessions_spawn(agentId="backend")` / `sessions_spawn(agentId="frontend")`
- 已記入 ERRORS.md 待補

## ~/.openclaw/ 目錄整理
- 新增 `~/.openclaw/repos/` 作為外部 repo 統一 clone 位置
- `python-skills` 從 `~/` 搬到 `~/.openclaw/repos/python-skills/`
- Skills symlink 路徑更新：`~/.openclaw/skills/* → ~/.openclaw/repos/python-skills/skills/*`
- 5 個過時 agent 目錄已清除（claude-code/frontend-dev/quant-dev/quant-monitor/quant-research）
- workspace/skills 自訂 skill 也 symlink 到根目錄（quant-workflow）

## openclaw-config 整合
- 新增 `workspace/openclaw-config/` 作為部署工具包
- `openclaw.template.json`（去敏版）+ `SETUP.md` + cron/jobs.json
- cron/jobs.json 用 symlink 讓 OpenClaw 讀取
- SETUP.md 統一說明新環境部署步驟

## Skills 整理
- 移除 `artifact-lifecycle-manager` skill
- 重命名 `workflow-consistency` → `quant-workflow`
- trigger-map.md 合併進 SKILL.md（單一檔案）
- 移除和 python/quant skill 重複的內容，只留專案專屬 convention
- 加入「回測效能建議」（向量化預處理、on_trading_iteration 不做重計算）

## AGENTS.md 優化
- 模型定義移出，改為「見 openclaw.json」
- Skill 安裝指令移到 SETUP.md，AGENTS.md 只保留連結
- Scope Isolation 改為規則導向（不綁具體路徑）

## openclaw.json 修正
- main agent model 改為 sonnet-4-6（預設省 token）
- models 清單補上 opus-4-6（讓 /model UI 可切換）
- 名稱統一為「墨熵」（IDENTITY.md 同步）

## 回測效能決策
- decisions/2026-03-26-backtest-performance-optimization.md 記錄完整評估
- 立即：向量化預處理 + H1 餵入（不綁定為硬規則）
- 中期：Parquet / Ray 並行
- 長期：Numba / Redis
