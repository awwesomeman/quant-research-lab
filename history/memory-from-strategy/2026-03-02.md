# 2026-03-02

- 初次認識使用者 Jason。
- 使用偏好：全程繁體中文。
- 任務方向：聚焦前沿科技趨勢與金融市場趨勢，目標為提升在市場中的獲利機會。
- 市場範圍：美股、台股、加密貨幣（Polymarket 暫列後續）。
- 風險偏好：中等偏積極。
- 交易週期：短線波段 + 中波段。
- 時間限制：希望低干擾，不要長時間盯盤。
- 資金規模：約 30 萬台幣。
- 明確要求：每次交易都要提供合理解釋與交易邏輯。
- 使用者希望我主動探索更多交易策略（可參考論壇與論文），不限條件型策略，也可含模型方法與多因子，先討論再落地。
- 使用者要求之後將交易紀錄與績效相關文字輸出優先轉成易讀表格。
- 使用者同意持續做穩健型研究，並要求有空時將策略框架先套用到比特幣（先用公開資料，後續再用其API驗證）。
- 策略命名已更新為：`TrendPullback_v1.0.0-H1-L-MXFR1`、`MultiFactorScore_v1.0.0-H1-L-TSI`、`MultiFactorScore_v1.0.0-H1-LS-BTCF`。
- 使用者同意將長期記憶分成多個 domain（quant/finance/productivity/tech-trends/price-comparison），並由我依情境自動分類，必要時可動態新增分類。
- 使用者要求後續 API 設定/權限/部署等交易系統工程相關內容由我自動處理與分類。
- 使用者要求進度回報需清楚標示狀態（執行中/待執行/等待決策），避免誤以為任務正在跑。
- 使用者希望回覆語氣更活潑，並帶批判性思考，避免機器人感。
- 助手命名為「墨熵」（量化合作情境使用）。
- 使用者要求維護一份代辦清單，詢問進度時需列項目並簡述狀態。
- 使用者要求建立「單一資產策略研究規劃清單」。
- 使用者要求策略命名規則為：`[StrategyName]_v[Major].[Minor]-[TF]-[Side]-[Asset]`（標的放最後，頻率偏好 `H1`/`M60`），並定義版本語意：Major=邏輯改變，Minor=參數調整。
- 版本管理規則補充：修 bug（timeout、資料抓取修正、rate-limit 重試等）不升版，直接覆蓋同版本並保留 bugfix 記錄；同策略在不同標的首次實驗可各自用 v1.0。
- Binance API 安全設定確認完成：`ipRestrict=true`、`enableReading=true`、`enableFutures=true`、交易/提幣關閉（只讀+白名單）。
- 待辦清單已升級為可追蹤格式（完成/進行中/待執行；每項含分派時間、完成時間、狀態描述）。
- `TrendPullback_v1.0.0-H1-L-BTC` 已完成初版回測與穩健性比較；目前維持 v1.0 為主版本（v1.1 候選未採用）。
- 已啟用 `TrendPullback_v1.0.0-H1-L-BTC` 自動監控與訊號通知（每 5 分鐘檢查）。
- 使用者要求所有回測回報必須嚴格套用「精簡版模板 + 詳細版模板」，避免格式跑掉。
- `TrendPullback_v1.0.0-H1-L-BTC` 與候選 `v1.1` 比較後，依 OOS 結果決定維持 v1.0（v1.1 未採用）。
- 使用者指出 BTC 監控頻率需與 TSI 一致（兩階段：整點 setup + 視窗內 1 分 trigger），不接受單純每 5 分鐘全量掃描。
- 使用者要求「精簡版比較」也必須維持一致模板欄位（至少含交易次數、勝率、平均報酬率等），不得過度省略。
- 使用者要求之後分析單資產策略回測時，固定從兩個模板（brief/full）擇一輸出，預設先給 brief。
